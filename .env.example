APP_BASE_URL=http://localhost:3000
COOKIE_SECRET=replace-with-a-long-random-string
KEY_ENCRYPTION_SECRET=
NODE_ENV=development
DATABASE_URL=

# Optional: known founder login keys managed by environment.
# Generate each key with:
# node -e "console.log('ak_'+require('crypto').randomBytes(32).toString('base64url'))"
FOUNDER_ACCESS_KEYS=You=ak_replace_with_generated_key,Partner=ak_replace_with_generated_key
